---
description: Planificar un cambio antes de tocar código (solo lectura)
---

Analizá el pedido: **$ARGUMENTS**

Trabajá en modo planificación (SOLO LECTURA — no edites archivos todavía):
1. Explorá el código relevante (motor en `mvdg/`, UI en `app/app.py`, API en `bi_api/`) y entendé el estado actual.
2. Identificá los archivos que habría que tocar y por qué. Si el cambio agrega texto de usuario, recordá la regla de i18n trilingüe (ES/EN/PT en `mvdg/i18n.py`).
3. Proponé un plan paso a paso, marcando riesgos y puntos de decisión.
4. Esperá aprobación antes de ejecutar.
